@model IEnumerable<Web_Odev6.Models.Entity.hastalik>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-bordered table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.hastalık)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.detay)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ilac_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.hasta_table.isim)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.hastalık)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.detay)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ilac_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.hasta_table.isim)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
